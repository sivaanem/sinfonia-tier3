[cli]
locustfile = "src/loadtest/locustfile.py"
rps_per_user = [50]
# num_workers = 3
run_time = "2m"
tspad_seconds = 0

[cli.local]
host = "http://localhost"
port = 8000

[cli.global]
host = "http://obelix30.cs.umass.edu"
port = 30080

[locust]
locustfile = "src/loadtest/locustfile.py"
headless = true
host = "http://localhost"
users = 6
# spawn-rate = 10
run-time = "2m"
processes = 6
expect-workers = 6
